@{

    var db = Database.Open("VmApp");
    var sql = "SELECT phone_number FROM UserName WHERE phone_number LIKE @0";
    var term = Request["term"] + "%";
    var result = db.Query(sql, term);
    var data = result.Select(p => new { label = p.phone_number });
    Json.Write(data, Response.Output);


}